<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/style.css">
    <style>
      .custom-search-form {
        background: #fff;
        border-radius: 25px;
        box-shadow: 0 2px 8px rgba(0,0,0,0.07);
        padding: 3px 8px;
      }
      .custom-search-input {
        border-radius: 25px !important;
        border: 1px solid #ddd;
        box-shadow: none;
        transition: border-color 0.2s;
      }
      .custom-search-input:focus {
        border-color: #fe424d;
        box-shadow: 0 0 0 0.1rem #fe424d33;
      }
      .custom-search-btn {
        border-radius: 25px !important;
        background-color: #fe424d !important;
        color: #fff !important;
        border: none;
        padding-left: 20px;
        padding-right: 20px;
        transition:  0.2s;
      }
      .custom-search-btn:hover, .custom-search-btn:focus {
        background-color: #d7373f !important;
        color: #fff !important;
      }
      .airbnb-btn {
        background: #fff;
        color: #fe424d !important;
        border: 1.5px solid #fe424d;
        border-radius: 25px;
        box-shadow: 0 2px 8px rgba(254,66,77,0.05);
        transition: 
          background 0.2s,
          color 0.2s,
          border-color 0.2s,
          box-shadow 0.2s;
      }
      .airbnb-btn:hover, .airbnb-btn:focus {
        background: #fe424d;
        color: #fff !important;
        border-color: #fe424d;
        box-shadow: 0 4px 16px rgba(254,66,77,0.12);
        text-decoration: none;
      }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top shadow-sm">
        <div class="container-fluid">
          <a class="navbar-brand fw-bold text-danger" href="/listings">
            <i class="fa-solid fa-compass"></i> Wanderslust
          </a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse d-flex justify-content-between align-items-center" id="navbarNavAltMarkup">
            <div class="navbar-nav">
              <% if (!currentUser) { %>
                <a class="nav-link fw-semibold" href="/signup"><i class="fa-solid fa-user-plus"></i> Signup</a>
                <a class="nav-link fw-semibold ms-2" href="/login"><i class="fa-solid fa-right-to-bracket"></i> Login</a>
              <% } %>
            </div>
            <div class="mx-auto" style="width: 400px; max-width: 100%;">
              <form class="d-flex custom-search-form" role="search">
                <input class="form-control me-2 custom-search-input" type="search" placeholder="Search Destinations" aria-label="Search"/>
                <button class="btn custom-search-btn" type="submit">Search</button>
              </form>
            </div>
            <div class="d-flex align-items-center">
              <a class="nav-link ms-2 px-4 py-2 fw-bold airbnb-btn" href="/listings/new">
                <i class="fa-solid fa-house-chimney"></i> Airbnb your home
              </a>
              <% if (currentUser) { %>
                <a class="nav-link ms-4 text-danger fw-semibold" href="/logout"><i class="fa-solid fa-right-from-bracket"></i> Logout</a>
              <% } %>
            </div>
          </div>
        </div>
      </nav>
</body>
</html>